#include <stdio.h>
int main()
{
    int prin, year, _jf();
    float SI, rof;
    const abcd;
    prin = 42;
    year = 4;
    rof = 10;

    /*Simple interst formula*/
    // Simple interst formula
    SI = prin * year * rof / 100;
    printf("%f\n", SI);

    return 0;
}